# 11 — About

**Status:** done

`/about` — identity. Sections:

1. **What Konneal is** — one paragraph: the serving layer of the
   suite; what it does to a corpus; who owns what.
2. **The name** — the three decodes: K-onneal (knowledge annealment,
   the published method); anneal stays legible; konne(ction) — the
   SDO connects to its members, questions to clauses, answers to the
   original document.
3. **The suite** — the mark decoded: three layered sheared slabs,
   structure being added to structure, each layer locked to the same
   shear — authored in Metanorma, modelled in Primmel, served by
   Konneal. Mark + domain lockup rendered.
4. **Lineage** — born from a live public service; the engine
   extracted with history preserved; the method proven by the
   measurement programme.
5. **Principles** — the publisher owns the content, the profile and
   the frontend; all models are open-weight; every release is
   measured before it ships; a citation is a place you can go.

## Acceptance

Nothing invented (no founding dates, no team names); mark rendered
from the official artwork; page links to How it works and
Deployments.
