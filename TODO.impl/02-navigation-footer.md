# 02 — Navigation and footer

**Status:** done

Desktop nav: `Product ▾` (Features, How it works) · `Solutions ▾`
(Use cases, Audiences) · Deployments · About — plus a Get started
button and the mono reference link `ai.oimlsmart.org ↗`. Dropdowns
open on click, close on outside click and Escape, stay open on
`:focus-within`; no mega-menu, no hover-only affordances.

Mobile: one disclosure (details) listing the groups and their items,
then Deployments, About, Get started, reference.

Footer: four sitemap columns — Product (Features, How it works,
Annealment), Solutions (Use cases, Audiences), Company (About,
Deployments, Get started), Reference (OIML SMART AI ↗, Metanorma ↗) —
with the domain lockup, the suite line, and a full-sentence tagline.

Annealment is reachable from nav nowhere; from Features, How it
works and the footer everywhere.

## Acceptance

Dropdowns keyboard-operable (toggle, Escape, tab into panel); active
page marked; all nine pages + reference reachable from every page via
header or footer.
