# 03 — Home as master index

**Status:** done

One scroll that routes to every page. Sections:

1. **Thesis** — "Konneal anneals your publications into a corpus a
   machine can traverse and compute." Lede: the suite frame, the
   passes, the served surface. One measured fact (2/18 vs 17–18/18)
   linking to /annealment/. CTAs: Get started (primary), How it works.
2. **What your members see** (`#what-members-see`) — the answer
   anatomy card (citation, verbatim value, typed rendering, computed
   verdict). Route: Use cases.
3. **What you already have** (`#what-you-have`) — the inventory
   ladder: asset → what your members already get. Baseline row
   Metanorma; additive rows Pubid, Relaton, Glossarist, Lutaml;
   execution row Primmel. Routes: Features, Get started.
4. **How that is possible** — the three slabs (authored, modelled,
   served); the mark decoded in one sentence. Route: How it works.
5. **Evidence** — one card: a public deployment you can question.
   One sentence, the instance link, route to Deployments. No stats.
6. **Closing band** (night) — "Start from what you already have." +
   Get started button.

The staircase diagram does not appear on Home (it lives on
Annealment). The playground does not appear on Home (it lives on Use
cases). Home carries one interactive-free argument plus the answer
card.

## Acceptance

Every section ends in a route line; no OIML numbers on the page; hero
is a complete sentence.
